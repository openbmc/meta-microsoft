# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILE_
BBFILE_COLLECTIONS += "olympus-layer"
BBFILE_PATTERN_olympus-layer = ""
LAYERSERIES_COMPAT_olympus-layer = "thud"
